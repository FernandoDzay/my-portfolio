


type Props = {
    children: React.ReactNode
}

const Body:React.FC<Props> = ({ children }) => {
    
    
    return (
        <body>
            
            {children}
        </body>
    )
}

export default Body
